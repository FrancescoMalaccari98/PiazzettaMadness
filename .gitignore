# .NET
bin/
obj/
.vs/
TestResults/
*.user
*.suo

# Pacchetto backend generato (Fase 8B) — artefatto binario, non committare
release/backend.zip

# VS Code local
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# Python
.venv/
.venv*/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Runtime generated files
artifacts/
runtime/Input/*
runtime/Working/*
runtime/OutputJson/*
runtime/Elaborati/*
runtime/Errori/*
runtime/Logs/*
runtime/Dataset/*
runtime/Debug/*

# Keep runtime folders
!runtime/Input/.gitkeep
!runtime/Working/.gitkeep
!runtime/OutputJson/.gitkeep
!runtime/Elaborati/.gitkeep
!runtime/Errori/.gitkeep
!runtime/Logs/.gitkeep
!runtime/Dataset/.gitkeep
!runtime/Debug/.gitkeep

# Samples must remain versionable
!samples/
!samples/pdf/

# Dati DB privati e PDF reali della validazione roster dinamico (Fase 10E): MAI committare.
# Contengono identità canoniche reali (matchId, teamId, playerId, nomi giocatori) e PDF di gara.
samples/private/

# Planning prompts
Prompt_pianificazione.txt

# Credenziali server — NON committare
backend/config/database.php

# Configurazione locale con segreti (token API, percorsi personali)
Config/appsettings.json
Config/appsettings.portable.json

# Configurazione locale Claude Code
CLAUDE.local.md
.claude/settings.local.json

# Dump SQL del DB con dati reali (Fase 10E): non committare i nuovi dump.
# NB: NON rimuove dal tracking i dump .sql già committati (git ignora questa regola
# per i file già tracciati). Serve solo a impedire che NUOVI dump vengano aggiunti.
database/*.sql

.venv-paddle/
.venv/
venv/
__pycache__/
*.pyc
bin/
obj/
.vs/
