{
  "_comment": "Copia in .claude/settings.local.json per sovrascrivere permessi su questa macchina. Non committare settings.local.json.",
  "permissions": {
    "allow": [
      "Bash(dotnet test * --filter *Integration*)"
    ]
  }
}
