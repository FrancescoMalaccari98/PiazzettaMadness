<Window x:Class="PiazzettaMadness.App.Forms.GameClockAdjustmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Imposta tempo partita"
        Width="360"
        Height="230"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Inserisci il tempo rimanente. Il cronometro verrà fermato."
                   TextWrapping="Wrap"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Minuti" HorizontalAlignment="Center" />
                <TextBox x:Name="MinutesTextBox"
                         Width="80"
                         Height="36"
                         Margin="0,4,0,0"
                         FontSize="20"
                         TextAlignment="Center"
                         PreviewTextInput="NumberTextBox_PreviewTextInput" />
            </StackPanel>
            <TextBlock Text=":" FontSize="28" VerticalAlignment="Bottom" Margin="10,0,10,2" />
            <StackPanel>
                <TextBlock Text="Secondi" HorizontalAlignment="Center" />
                <TextBox x:Name="SecondsTextBox"
                         Width="80"
                         Height="36"
                         Margin="0,4,0,0"
                         FontSize="20"
                         TextAlignment="Center"
                         PreviewTextInput="NumberTextBox_PreviewTextInput" />
            </StackPanel>
        </StackPanel>

        <TextBlock x:Name="ValidationText"
                   Grid.Row="2"
                   Foreground="#b91c1c"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,0" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annulla" Width="90" Height="34" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Imposta" Width="90" Height="34" IsDefault="True" Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>
